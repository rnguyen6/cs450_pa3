NAME
	uv2p – Writes physical address of a given virtual address (excluding 0x prefix) to the standard ouput.

SYNOPSIS
	uv2p [arg ...]

DESCRIPTION
	Writes physical address of a given virtual address (excluding 0x prefix) to the standard ouput.

	Displays the physical address on the standard output followed by a newline.

EXIT STATUS
	int representing physical address
		Virtual addresses will always be translated, but invalid addresses will be prefixed as invalid.
